---
import BaseLayout from '../layouts/BaseLayout.astro';

const experience = [
  {
    period: '2023 — Present',
    role: 'Indie App Developer',
    org: 'Independent',
  },
  {
    period: '2021 — 2023',
    role: 'iOS Developer',
    org: 'Company Name',
  },
  {
    period: '2019 — 2021',
    role: 'Software Engineer',
    org: 'Company Name',
  },
];

const skills = [
  { name: 'Swift · SwiftUI', desc: 'iOS / macOS native' },
  { name: 'Kotlin · Jetpack Compose', desc: 'Android native' },
  { name: 'TypeScript · Astro', desc: 'Web & static sites' },
  { name: 'Figma', desc: 'UI design & prototyping' },
];
---

<BaseLayout title="About" description="About insong — indie app developer">
  <!-- Header -->
  <section class="py-24 md:py-36 px-6">
    <div class="max-w-5xl w-full mx-auto">
      <div class="max-w-xl animate-reveal">
        <span class="section-label">About</span>
        <h1 class="text-3xl md:text-4xl font-semibold text-text tracking-tight mb-4">
          insong
        </h1>
        <p class="text-text-sub leading-relaxed">
          I'm an indie app developer focused on iOS and mobile products.
          I've been shipping apps since 2019 — I believe the best software
          is opinionated, fast, and gets out of your way.
        </p>
      </div>
    </div>
  </section>

  <!-- Experience -->
  <section class="py-20 px-6 border-t border-border bg-bg-alt">
    <div class="max-w-5xl mx-auto">
      <div class="mb-8 animate-reveal">
        <span class="section-label">Experience</span>
        <h2 class="text-2xl font-semibold text-text tracking-tight">Work</h2>
      </div>
      <div class="flex flex-col md:flex-row gap-16 items-start">
        <div class="flex-1 animate-reveal delay-1">
          <div class="divide-y divide-border">
            {experience.map(item => (
              <div class="flex flex-col sm:flex-row sm:items-baseline gap-1 sm:gap-4 py-4 first:pt-0 last:pb-0">
                <span class="text-xs text-text-sub tabular-nums shrink-0 w-32">{item.period}</span>
                <div class="flex-1">
                  <div class="text-sm font-semibold text-text">{item.role}</div>
                  <div class="text-sm text-text-sub">{item.org}</div>
                </div>
              </div>
            ))}
          </div>
        </div>
        <div class="flex-1 animate-reveal delay-2">
          <p class="text-sm text-text-sub leading-relaxed">
            I started as a software engineer and moved into mobile development.
            Now I work independently — building, shipping, and iterating on my own apps.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Skills -->
  <section class="py-20 px-6 border-t border-border bg-bg">
    <div class="max-w-5xl mx-auto">
      <div class="mb-8 animate-reveal">
        <span class="section-label">Tools</span>
        <h2 class="text-2xl font-semibold text-text tracking-tight">Skills & Stack</h2>
      </div>
      <div class="grid sm:grid-cols-2 gap-3 animate-reveal delay-1">
        {skills.map(skill => (
          <div class="flex items-center gap-3 bg-bg-alt border border-border rounded-xl px-4 py-3.5">
            <div>
              <div class="text-sm font-medium text-text">{skill.name}</div>
              <div class="text-xs text-text-sub">{skill.desc}</div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>
</BaseLayout>
